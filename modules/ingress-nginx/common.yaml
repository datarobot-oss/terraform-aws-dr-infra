controller:
  allowSnippetAnnotations: true
  admissionWebhooks:
    enabled: false
  config:
    use-forwarded-headers: "true"
    use-proxy-protocol: "true"
  replicaCount: 3
  service:
    targetPorts:
      https: http
